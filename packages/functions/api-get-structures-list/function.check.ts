// tslint:disable-next-line
console.time("check");
(async () => {
    // Check event function

})().then(() => {
    // tslint:disable-next-line
    console.timeEnd("check");
}).catch((e) => {
    // tslint:disable-next-line
    console.log(e);
});
